syntax = "proto3";

message SearchResponse {
  // protolint:disable:next PROTO3_GROUPS_AVOID
  repeated group Results = 1 {
    string url = 2;
    optional string title = 3;
    repeated string snippets = 4;
  }
}
