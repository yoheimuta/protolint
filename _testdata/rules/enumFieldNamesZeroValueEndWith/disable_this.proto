syntax = "proto3";

enum enum {
    option allow_alias = true;
    UNKNOWN = 0; // protolint:disable:this ENUM_FIELD_NAMES_ZERO_VALUE_END_WITH
}

enum enumAlias {
    STARTED_LAP = 1;
    RUNNINGLAP_UNTIL = 2 [(custom_option) = "hello world"];
}
