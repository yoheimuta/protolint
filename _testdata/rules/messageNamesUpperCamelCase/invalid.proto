syntax = "proto3";

message first_outer {
    option (my_option).a = true;
    // inner is an inner message.
    message first_inner {   // Level 2
                      int64 ival = 1;
    }
    message accountForAdmin {}
    message Special_EndOfSupport {}
}

message second {
    message account_for_admin {}
}
