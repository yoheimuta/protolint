syntax = "proto3";

enum enum {
    option allow_alias = true;
    ENUM_UNKNOWN = 0;
}

enum enumAlias {
    ENUM_ALIAS_STARTED_LAP = 1;
    ENUM_ALIAS_RUNNINGLAP_UNTIL = 2 [(custom_option) = "hello world"];
}
