syntax = "proto3";

enum enum {
    option allow_alias = true;
    UNKNOWN = 0; // protolint:disable:this ENUM_FIELD_NAMES_PREFIX
}

enum enumAlias {
    STARTED_LAP = 1; // protolint:disable:this ENUM_FIELD_NAMES_PREFIX
    RUNNINGLAP_UNTIL = 2 [(custom_option) = "hello world"]; // protolint:disable:this ENUM_FIELD_NAMES_PREFIX
}
